watchdog==4.0.1
boto3>=1.34
python-dotenv==1.0.1

# Legacy Google Drive deps (kept in case old clips need re-uploading)
google-api-python-client==2.131.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# Optional: tray icon (requires Pillow, which lacks Python 3.14 wheels as of now)
# Install manually once Pillow supports your Python version:
#   pip install pystray Pillow

